export enum RoleEnum {
  Admin = 1,
  Technician = 2,
  BrandManager = 3,
  ShopManager = 4,
  Employee = 5,
}

export enum AccountStatus {
  New = 1,
  Active = 2,
  Inactive = 3,
}

export enum BrandStatus {
  Active = 1,
  Inactive = 2,
}
